{
  "name": "codex-grok-build",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "verify:cursor": "node scripts/verify-cursor.mjs"
  }
}
